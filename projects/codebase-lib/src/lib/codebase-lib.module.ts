import { NgModule } from '@angular/core';
import { CodebaseLibComponent } from './codebase-lib.component';

@NgModule({
  declarations: [CodebaseLibComponent],
  imports: [
  ],
  exports: [CodebaseLibComponent]
})
export class CodebaseLibModule { }
